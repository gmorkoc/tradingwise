# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Environment variables
.env
.env.local
.env.*.local
# Local Supabase edge function secrets (e.g. supabase/.env.local.functions) —
# .env.*.local above doesn't match this shape (it ends in .functions, not
# .local), and it was sitting untracked with a real OpenAI key in it.
.env.local.*
